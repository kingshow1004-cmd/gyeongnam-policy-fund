# 정책자금 만들기 (정책자금 자료 만들기) 작업 공간

이 폴더는 **경남 정책자금 안내 앱** 프로젝트의 전용 작업 공간입니다.

## 저장 규칙 (중요)
- "정책자금 만들기", "정책자금 만들기 일" 세션에서 생성·수정하는 **모든 파일은 반드시 이 폴더
  (`코딩용\정책자금자료만들기\`) 안에 저장**합니다.
- 새 파일, 배포 자료, 메모, 백업 등 예외 없이 이 폴더 기준으로 만듭니다.
- 다른 프로젝트(자비스 만들기, 계산기 등) 파일은 이 폴더에 두지 않습니다.

## 현재 구성 파일
- `index.html` — 앱 본체 (UI + 클라이언트 로직, 모바일 반응형/탭 포함)
- `정책자금-서버.js` — 로컬 정적 서버 (Node, 포트 3001)
- `정책자금앱-실행.bat` — 서버 실행 + 브라우저 열기 (자기 폴더 기준 실행)
- `배포방법.md` — 배포 가이드 (Netlify Drop 등)

## 실행 방법
`정책자금앱-실행.bat` 더블클릭 → `cd /d "%~dp0"`로 이 폴더로 이동 후
`node 정책자금-서버.js` 실행 → http://localhost:3001 자동 오픈.
서버와 `index.html`이 같은 폴더에 있어야 정상 작동합니다.
