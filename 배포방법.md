# 🚀 경남 정책자금 안내 — 배포 가이드

## 한 줄 요약
**`index.html` 파일 하나만 인터넷에 올리면 끝.** 서버도, 비용도 필요 없습니다.

---

## ✅ 가장 쉬운 방법: Netlify Drop (5분, 무료, 회원가입 불필요)

1. 브라우저에서 **https://app.netlify.com/drop** 접속
2. `index.html` 파일을 **화면으로 드래그**
3. 끝! `https://랜덤이름.netlify.app` 주소가 바로 생성됨
4. 이 주소를 **카톡으로 공유**하면 누구나 접속 가능

> 주소를 예쁘게 바꾸려면(예: `gyeongnam-fund.netlify.app`): 무료 회원가입 후 Site settings → Change site name

---

## 🔷 방법 2: Vercel (커스텀 도메인·관리 편의)

1. **https://vercel.com** 가입 (GitHub 계정 추천)
2. 새 프로젝트 → `index.html`이 든 폴더 업로드 또는 GitHub 연동
3. 자동 배포 → `https://프로젝트명.vercel.app` 생성

> 이 폴더를 그대로 올리면 됩니다. 별도 설정 불필요 (정적 사이트 자동 인식).

---

## 📱 방법 3: GitHub Pages (개발자용)

```bash
# 이 폴더에서
git add index.html
git commit -m "배포"
git push
# GitHub 저장소 → Settings → Pages → Branch 선택 → Save
```

---

## 💡 폰 홈화면 앱처럼 쓰기 (PWA 느낌)

배포된 주소를 폰 브라우저로 열고:
- **아이폰**: 공유 → "홈 화면에 추가"
- **안드로이드**: 메뉴 → "홈 화면에 추가"

→ 아이콘이 생기고 앱처럼 실행됩니다.

---

## ⚙️ 작동 원리 (왜 서버가 필요 없나?)

- 모든 자금 데이터와 판별 로직이 `index.html` 안에 들어있음
- 실시간 공고는 **기업마당 정부 API를 브라우저가 직접 호출** (CORS 허용 확인됨)
- 사용자가 입력한 정보는 **어디에도 전송되지 않고** 브라우저 안에서만 계산됨
- → 개인정보 문제 없음, 서버 비용 0원, 내 PC 꺼도 작동

---

## 🔧 데이터 업데이트가 필요할 때

정책자금 조건이 바뀌면 `_dev/` 폴더의 스크래핑 스크립트를 다시 돌려서
`index.html`의 데이터 부분을 갱신하면 됩니다. (분기별 1회 권장)

- `_dev/deep-scrape2.js` — 소진공 자금 조건 스크래핑
- `_dev/scrape-schedule.js` — 신청 기간 스크래핑

---

## ⚠️ 배포 전 체크리스트

- [x] 단일 파일로 작동 (서버 불필요)
- [x] 모바일 반응형 적용
- [x] 법적 면책 문구 포함
- [x] 개인정보 미수집 (브라우저 내 처리)
- [x] 카톡 공유 미리보기 메타태그
- [ ] **기업마당 API 키**: 현재 `index.html`에 무료 조회키가 포함됨.
      대량 배포 시 별도 키 발급 권장 (data.go.kr 또는 bizinfo.go.kr)
